var React = require('react');

var App = React.createClass({

  render: function () {
    return (
      <div className="hello">
        Hello Alarm !!
      </div>
    );
  }

});

module.exports = App;
